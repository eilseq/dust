[package]
name = "event_parser"
version = "0.1.0"
edition = "2018"

[dependencies]
regex = "1.11.1"
serde = { version = "1.0.217", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
embedded_tidal_core = { path = "../embedded_tidal_core" }
